name: 🚀 Deploy via FTP

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: 🚚 Get latest code
        uses: actions/checkout@v4

      - name: 📂 Upload files via FTP
        uses: SamKirkland/FTP-Deploy-Action@v4.3.5
        with:
          server: niclacombe.ca                        # same secret for hostname
          username: dh_ci77k2                          # same secret for username
          password: *AnGoRu*jJ                         # same secret for password
          protocol: ftp                                # 🔥 switch from sftp to ftp
          port: 21                                     # standard explicit FTPS port
          server-dir: /niclacombe.ca/test/             # remote folder (adjust if needed)
          local-dir: ./                                # folder to upload (adjust if you want ./build or another folder)
